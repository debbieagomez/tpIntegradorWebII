extends ../layout

block content

  if pacientes.length === 0
    p No hay pacientes registrados.
  else
    ul
      each paciente in pacientes
        li
          | #{paciente.nombre} - DNI: #{paciente.dni} - Sexo: #{paciente.sexo}
          a(href=`/paciente/${paciente.id}/editar`) 🖊 Editar
          form(method='POST', action=`/paciente/${paciente.id}/eliminar`, style='display:inline')
            button(type='submit') ❌ Eliminar



